apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: postgres-controller-controller-manager-metrics-service
  namespace: postgres-controller-system
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    control-plane: controller-manager
